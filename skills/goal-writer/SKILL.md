---
name: goal-writer
description: 生成可交给 Agent 自主执行的 Goal。适用于用户准备把某个明确目标交给 AI 跑完，需要 Codex 根据当前上下文写出目标、完成标准和验收方式，避免目标含糊、验收不可验证或 Agent 跑偏的场景。
---

# Goal 编写技能

把下一步写成 Agent 可以自主执行的目标。

## 核心原则

Goal 不是愿望。Goal 必须单一、可执行、可验收。

## Goal 必须包含

- 目标
- 完成标准
- 验收方式

## 红线

- 不写“完善一下”“优化一下”“全部实现”这种目标。
- 不把多个互相依赖的大目标塞进一条 Goal。
- 不省略验收证据。
- 不用不可观察标准，例如“体验更好”。

